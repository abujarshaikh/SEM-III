//slip no.2,9
//que no.1
//length of a string 
#include<stdio.h>
#include<string.h>
int main(){
char s[100];
int len=0;
printf("Enter the string: ");
fgets(s,100,stdin);
printf("The string is:-");
len=strlen(s);
puts(s);
printf("THe length of the string is ::%d",len);
return 0;
}
