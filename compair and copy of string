#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include<string.h>
void main()
{
char s_1[50],s_2[50];
printf("Enter the string:: ");
scanf("%s",s_1);
//strcpy(s_2,s_1)
//abujar
printf("The copied string is::%s",strcpy(s_2,s_1));
printf("\nEnter the string::");
scanf("%s",s_2);
if(strcmp(s_2,s_1)==0)
{
    printf("Strings are same");
}
else
{
    printf("Strings are doesn't same");
}
//printf("\n\nAfter the swapnig\nThe value of a=%d\nThe value of b=%d",*x,*y);
/*sum=*ptr_a+*ptr_b; 
printf("\nSum = %d",sum);*/
getch();
}
